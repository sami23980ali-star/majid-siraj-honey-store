# نشر متجر «ماجد سراج»

آخر تحقق: 14 أغسطس 2026 — `tsc --noEmit` و`vitest run` (124 ناجحًا، 1 متخطى) و`vite build` والإقلاع بحزمة الإنتاج، كلها سليمة. المخطط والهجرات متوافقان («No schema changes, nothing to migrate»).

## 1. متغيّرات البيئة

انسخ [.env.example](.env.example) إلى `.env` على الخادم واملأه. لا تضع سرًّا في أي متغيّر يبدأ بـ `VITE_` لأنه يُحقن في حزمة المتصفح.

الحد الأدنى لمتجر حقيقي: `DATABASE_URL` و`JWT_SECRET` (قيمة عشوائية طويلة خاصة بالإنتاج) و`OAUTH_SERVER_URL` و`OWNER_OPEN_ID` و`BUILT_IN_FORGE_API_URL/KEY`. الحارس في [envGuard.ts](server/_core/envGuard.ts) يوقف الإقلاع في الإنتاج إن غاب `JWT_SECRET`، ويطبع تحذيرًا واضحًا لكل متغيّر ناقص.

## 2. الهجرة والبناء والتشغيل

```bash
pnpm install
pnpm db:push        # drizzle-kit generate && migrate — يحتاج DATABASE_URL
pnpm build          # vite build + حزمة الخادم في dist/
pnpm start          # NODE_ENV=production node dist/index.js
```

## 3. أول حساب مدير

للحساب الأول إثباتان مقبولان فقط:

- **المفضّل:** املأ `OWNER_OPEN_ID` بمعرّفك، ثم افتح `/admin` وسجّل دخول مالك المشروع، فتظهر شاشة إعداد اسم المستخدم وكلمة المرور.
- **بديل:** اضبط `ADMIN_SETUP_TOKEN` بقيمة عشوائية طويلة، واكتبها في نموذج الإعداد. **احذف المتغيّر بعد إنشاء الحساب** — ما دام مهيّأً فأي حساب موثّق يعرف الرمز يستطيع إنشاء أول مدير.

إن بقي المتغيّران فارغين يُرفض الجميع، ولن يُنشأ حساب مدير. بعد ذلك تُدار الحسابات من تبويب «المستخدمون» (للمالك فقط) بأدوار owner/manager/editor.

## 4. تحقق بعد النشر (يُنفَّذ على النطاق الحي)

أُغلقت البنود المعتمدة على قاعدة البيانات محليًا مقابل MySQL حقيقية (التفاصيل في [verification_notes.md](verification_notes.md)): رحلة الطلب والمخزون والتتبع، ومراجعة مرتبطة بطلب حقيقي من الإرسال إلى الاعتماد إلى الظهور العلني، وحالات بوابة `/admin` الأربع في متصفح فعلي. الباقي يحتاج النطاق الحي:

1. `/admin` بجلسة مالك المشروع عبر **OAuth حقيقية**: العودة من مزوّد الهوية إلى `/admin` نفسه لا إلى الصفحة الرئيسية. (محليًا جُرِّبت جلسة المالك بكوكي JWT موقّع، وهي تثبت البوابة لا رحلة OAuth.)
2. الدفع الإلكتروني: مطالبة متجر Shopify وإعداد مزود دفع فعلي من إعداداته، ثم ضبط `SHOPIFY_STORE_DOMAIN` و`SHOPIFY_STOREFRONT_API_ACCESS_TOKEN`. قبل ذلك يبقى الطلب عبر واتساب فقط ويُخفى زر الدفع.

تحقق تشغيلي مطلوب مرة واحدة بعد أول نشر بهذا الإصدار: أن الهجرة `0009` طُبِّقت فعلًا (`lockedUntil` و`expiresAt` من نوع `datetime` لا `timestamp`)، وأن أول دخول لحساب مدير محلي جديد ينجح من المحاولة الأولى — فشلٌ برسالة قفل زمني طويل يعني أن الهجرة لم تُطبَّق.

## 5. ما لا يعمل بغير تهيئة

- بلا `DATABASE_URL`: الكتالوج فارغ، و`adminAuth.status` يجيب 503، وتظهر في `/admin` شاشة «تعذر التحقق من صلاحية الدخول» مع زر إعادة المحاولة (بعد استنفاد إعادات المحاولة، نحو عشر ثوانٍ).
- بلا مفاتيح `BUILT_IN_FORGE_*`: مسار `/manus-storage/*` يجيب 500، فلا تظهر صور المنتجات ولا صورة بطاقة المشاركة.
- بلا `OAUTH_SERVER_URL`: دخول مالك المشروع لا يعمل، ويسجّل الخادم ذلك عند الإقلاع.
